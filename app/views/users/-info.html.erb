
      <h2>User info</h2>
        <!--%= users.profile_image %>-->
      <table class="table">
        <tbody>
          <tr>
            <th>name</th>
            <td><%= user.name %></td>
          </tr>
          <tr>
            <th>introduction</th>
            <td><%= user.introduction %></td>
          </tr>
        </tbody>
      </table>

      <div class="row">
        <a class="btn btn-outline-secondary btn-block">
          <%= link_to edit_user_path(user), class: "fa fa-user-cog" %>
        </a>
      </div>