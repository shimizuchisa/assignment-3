<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
          <a class="navbar-brand" href="/"><span>Bookers</span></a>
          <!--ナビゲーションバーはclassに「navbar」「navbar-expand-〇〇」「navbar-色名」「bg-色名」-->
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
              <% if user_signed_in? %>
            <!--mr-auto は右側に、.ml-auto は左側に最大限のマージンをつける-->
            <!-- %= link_to URLヘルパー, class: "btn btn-danger", method: :delete do %
              <i class="far fa-trash-alt"></i> =>アイコン
            % end %-->
              <li class="nav-item">
                <%= link_to user_path(current_user.id), class: "nav-link text-light" do%>
                  <i class="fa fa-home"></i><span> Home</span>
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to users_path, class: "nav-link text-light" do %>
                  <i class="fa fa-users"></i><span> Users</span>
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to books_path, class: "nav-link text-light" do %>
                 <i class="fa fa-book-open"></i><span> Books</span>
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to destroy_user_session_path, method: :delete, class: "nav-link text-light" do %>
                  <i class="fa fa-sign-out-alt"></i><span> Log out</span>
                <% end %>
              </li>
              <% else %>
              <li class="nav-item">
                <%= link_to  root_path, class: "nav-link text-light" do %>
                  <i class="fa fa-home"></i><span> Home</span>
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to homes_about_path, class: "nav-link text-light" do %>
                  <i class="fa fa-link"></i><span> About</span>
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to new_user_registration_path, class: "nav-link text-light" do %>
                  <i class="fa fa-user-plus"></i><span> Sign up</span>
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to new_user_session_path, class: "nav-link text-light" do %>
                  <i class="fa fa-sign-in-alt"></i><span> Log in</span>
                <% end %>
              </li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>

    <%= yield %>

    <footer>
      <div class="container mt-5">
        <div class="row">
          <div class="mx-auto">
            <p>CopyRight Infratop.inc</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
